# Docker Environment Variables
# Copy this file to .env in the same directory
# These values are used by docker-compose.yml

# PostgreSQL Configuration
POSTGRES_USER=prismify
POSTGRES_PASSWORD=prismify_dev_password_change_me
POSTGRES_DB=prismify_dev

# Redis Configuration
REDIS_PASSWORD=redis_dev_password_change_me

# MinIO Configuration (Optional - only if using MinIO)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123_change_me

# PgAdmin Configuration (Optional - only if using admin tools)
PGADMIN_EMAIL=admin@prismify.local
PGADMIN_PASSWORD=admin123_change_me
